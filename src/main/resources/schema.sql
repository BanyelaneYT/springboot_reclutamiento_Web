CREATE TABLE IF NOT EXISTS evento (
                                      id INT AUTO_INCREMENT PRIMARY KEY,
                                      nombre VARCHAR(255),
    tipo VARCHAR(255),
    fecha VARCHAR(100),
    descripcion VARCHAR(255)
    );